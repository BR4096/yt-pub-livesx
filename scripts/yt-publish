#!/usr/bin/env bash
# yt-publish — Upload videos to YouTube via API
# Uses gws CLI credentials
#
# Usage:
#   yt-publish <file_or_drive_id> --title "Title" --description "Description" [options]
#
# Examples:
#   yt-publish ~/videos/lesson.mp4 --title "Lesson 1" --description "First lesson"
#   yt-publish --drive "1abc123fileId" --title "Lesson 2" --description "From Drive"
#   yt-publish video.mp4 --title "Test" --description "Desc" --privacy unlisted --tags "tag1,tag2"
#   yt-publish video.mp4 --title "Test" --description "Desc" --category 27 --language pt

set -euo pipefail

# --- Config (reads from .env) ---
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ENV_FILE="${GWS_CONFIG_DIR:-${SCRIPT_DIR}/../config}/.env"
if [[ -f "$ENV_FILE" ]]; then
  set -a; source "$ENV_FILE"; set +a
fi
GWS_CONFIG_DIR="${GWS_CONFIG_DIR:-${SCRIPT_DIR}/../config}"
CLIENT_ID="${CLIENT_ID:?Error: CLIENT_ID not defined. Check $ENV_FILE}"
CLIENT_SECRET="${CLIENT_SECRET:?Error: CLIENT_SECRET not defined. Check $ENV_FILE}"
API_KEY="${API_KEY:?Error: API_KEY not defined. Check $ENV_FILE}"
GWS_BIN="${HOME}/.npm-global/bin/gws"

# --- Defaults ---
PRIVACY="public"
TAGS=""
CATEGORY="22"  # People & Blogs
LANGUAGE="pt"
DRIVE_ID=""
FILE_PATH=""
TITLE=""
DESCRIPTION=""

# --- Parse args ---
while [[ $# -gt 0 ]]; do
  case "$1" in
    --drive)      DRIVE_ID="$2"; shift 2 ;;
    --title)      TITLE="$2"; shift 2 ;;
    --description) DESCRIPTION="$2"; shift 2 ;;
    --privacy)    PRIVACY="$2"; shift 2 ;;
    --tags)       TAGS="$2"; shift 2 ;;
    --category)   CATEGORY="$2"; shift 2 ;;
    --language)   LANGUAGE="$2"; shift 2 ;;
    --help|-h)
      head -14 "$0" | tail -12
      echo ""
      echo "Options:"
      echo "  --title       Video title (required)"
      echo "  --description Video description (required)"
      echo "  --drive       Google Drive file ID (instead of local file)"
      echo "  --privacy     public|unlisted|private (default: public)"
      echo "  --tags        Comma-separated tags"
      echo "  --category    YouTube category ID (default: 22 = People & Blogs)"
      echo "  --language    Video language (default: pt)"
      echo ""
      echo "Common categories: 1=Film, 2=Autos, 10=Music, 15=Pets, 17=Sports,"
      echo "  20=Gaming, 22=People&Blogs, 24=Entertainment, 25=News, 26=Howto, 27=Education, 28=Science"
      exit 0
      ;;
    -*)
      echo "Error: unknown option: $1" >&2
      exit 1
      ;;
    *)
      FILE_PATH="$1"; shift ;;
  esac
done

# --- Validation ---
if [[ -z "$TITLE" ]]; then
  echo "Error: --title is required" >&2
  exit 1
fi

if [[ -z "$DESCRIPTION" ]]; then
  echo "Error: --description is required" >&2
  exit 1
fi

if [[ -z "$FILE_PATH" && -z "$DRIVE_ID" ]]; then
  echo "Error: provide a local file or --drive <fileId>" >&2
  exit 1
fi

# --- Funções ---
get_access_token() {
  python3 << 'PYEOF'
import json, base64, urllib.request, urllib.parse
from cryptography.hazmat.primitives.ciphers.aead import AESGCM
import os

config_dir = os.environ.get('GWS_CONFIG_DIR', os.path.expanduser('~/.config/gws'))

with open(os.path.join(config_dir, '.encryption_key'), 'r') as f:
    key = base64.b64decode(f.read().strip())

with open(os.path.join(config_dir, 'credentials.enc'), 'rb') as f:
    data = f.read()

aesgcm = AESGCM(key)
creds = json.loads(aesgcm.decrypt(data[:12], data[12:], None))

token_data = urllib.parse.urlencode({
    'client_id': os.environ['CLIENT_ID'],
    'client_secret': os.environ['CLIENT_SECRET'],
    'refresh_token': creds['refresh_token'],
    'grant_type': 'refresh_token'
}).encode()

req = urllib.request.Request('https://oauth2.googleapis.com/token', data=token_data)
resp = json.loads(urllib.request.urlopen(req).read())
print(resp['access_token'])
PYEOF
}

echo "==> Getting access token..."
export CLIENT_ID CLIENT_SECRET GWS_CONFIG_DIR
ACCESS_TOKEN=$(get_access_token)

if [[ -z "$ACCESS_TOKEN" || "$ACCESS_TOKEN" == "ERROR"* ]]; then
  echo "Error: could not obtain access token" >&2
  exit 1
fi
echo "    Token OK"

# --- Download from Drive if needed ---
if [[ -n "$DRIVE_ID" ]]; then
  echo "==> Downloading file from Google Drive (ID: $DRIVE_ID)..."
  TMPFILE=$(mktemp /tmp/yt-upload-XXXXXX.mp4)

  export PATH="$HOME/.npm-global/bin:$HOME/google-cloud-sdk/bin:$PATH"
  $GWS_BIN drive files get --params "{\"fileId\": \"$DRIVE_ID\", \"alt\": \"media\"}" --output "$TMPFILE" 2>/dev/null

  if [[ ! -s "$TMPFILE" ]]; then
    echo "Error: failed to download file from Drive" >&2
    rm -f "$TMPFILE"
    exit 1
  fi

  FILE_PATH="$TMPFILE"
  FILE_SIZE=$(stat -c%s "$FILE_PATH")
  echo "    Downloaded: $FILE_PATH ($FILE_SIZE bytes)"
fi

# --- Verify file ---
if [[ ! -f "$FILE_PATH" ]]; then
  echo "Error: file not found: $FILE_PATH" >&2
  exit 1
fi

FILE_SIZE=$(stat -c%s "$FILE_PATH")
echo "==> Arquivo: $FILE_PATH ($FILE_SIZE bytes)"

# --- Build metadata ---
TAGS_JSON="[]"
if [[ -n "$TAGS" ]]; then
  TAGS_JSON=$(python3 -c "import json; print(json.dumps('$TAGS'.split(',')))")
fi

METADATA=$(python3 -c "
import json
meta = {
    'snippet': {
        'title': '''$TITLE''',
        'description': '''$DESCRIPTION''',
        'tags': $TAGS_JSON,
        'categoryId': '$CATEGORY',
        'defaultLanguage': '$LANGUAGE',
        'defaultAudioLanguage': '$LANGUAGE'
    },
    'status': {
        'privacyStatus': '$PRIVACY',
        'selfDeclaredMadeForKids': False
    }
}
print(json.dumps(meta))
")

echo "==> Starting upload to YouTube..."
echo "    Title: $TITLE"
echo "    Privacy: $PRIVACY"
echo "    Category: $CATEGORY"

# --- Resumable upload ---
# Step 1: Initiate upload
UPLOAD_URL=$(curl -s -X POST \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json; charset=UTF-8" \
  -H "X-Upload-Content-Length: $FILE_SIZE" \
  -H "X-Upload-Content-Type: video/*" \
  -D - \
  -d "$METADATA" \
  "https://www.googleapis.com/upload/youtube/v3/videos?uploadType=resumable&part=snippet,status&key=$API_KEY" \
  2>/dev/null | grep -i "^location:" | tr -d '\r' | sed 's/location: //i')

if [[ -z "$UPLOAD_URL" ]]; then
  echo "Error: failed to initiate resumable upload" >&2
  exit 1
fi

echo "    Upload URL obtained"

# Step 2: Upload file
echo "    Sending file..."
RESPONSE=$(curl -s -X PUT \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: video/*" \
  -H "Content-Length: $FILE_SIZE" \
  --connect-timeout 30 \
  --max-time 600 \
  --data-binary "@$FILE_PATH" \
  "$UPLOAD_URL")

# --- Parse response ---
VIDEO_ID=$(echo "$RESPONSE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('id',''))" 2>/dev/null || true)

if [[ -n "$VIDEO_ID" ]]; then
  echo ""
  echo "==> Upload complete!"
  echo "    Video ID: $VIDEO_ID"
  echo "    URL: https://www.youtube.com/watch?v=$VIDEO_ID"
  echo "    Studio: https://studio.youtube.com/video/$VIDEO_ID/edit"
else
  echo ""
  echo "Error during upload. API response:" >&2
  echo "$RESPONSE" | python3 -m json.tool 2>/dev/null || echo "$RESPONSE"
  exit 1
fi

# Cleanup temp file from Drive
if [[ -n "$DRIVE_ID" && -f "$TMPFILE" ]]; then
  rm -f "$TMPFILE"
fi
